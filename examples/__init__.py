"""Runnable examples for ``embodiment``.

A package only so the tests can import the demo host by name; every example
here is also runnable directly, e.g. ``python examples/greenhouse.py --help``.
Nothing in this directory ships in the wheel (``pyproject.toml`` packages
``embodiment`` only) — these are teaching hosts, not library code.
"""
